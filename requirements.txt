google-genai==2.6.0
pyaudio
python-osc
pyyaml
ruamel.yaml
pygame
numpy
scipy
pedalboard
ultralytics
lap>=0.5.12
bettercam; sys_platform == "win32"
opencv-python
mss
aiohttp
beautifulsoup4
Pillow
# torch: defaults to the CUDA build on linux (large) or cpu on win/mac. to force
# a build, install it first from the matching index, then run this file:
#   cpu:   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#   cuda:  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
torch
torchvision
torchaudio
requests
httpx
soundfile
stream2sentence
google-cloud-texttospeech
transformers
psutil
colorama
discord.py-self
fastapi
uvicorn[standard]
python-multipart
pynput
pyotp
pytz
nltk
websockets
chromadb
pymongo
mutagen
protobuf==3.20.3
imageio-ffmpeg
# local backend (only needed if backend: "local" in config.yml). NOT installed
# by default. grab it with:  .\bin\uv.exe pip install moonshine-voice==0.0.59
# 0.0.59 is the latest with prebuilt windows wheels; newer releases are
# linux only. (silero VAD is pulled at runtime via torch.hub, no pip needed.)
